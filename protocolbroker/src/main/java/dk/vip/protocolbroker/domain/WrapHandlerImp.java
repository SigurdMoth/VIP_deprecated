package dk.vip.protocolbroker.domain;

import dk.vip.protocolbroker.presentation.IWrapHandler;

public class WrapHandlerImp implements IWrapHandler {

    @Override
    public String handleWrap(String requestBody) {
        // Unwrap wrap to expression

        // Extract protocol from expression

        // Forward to correct protocol application

        return null;
    }
}
